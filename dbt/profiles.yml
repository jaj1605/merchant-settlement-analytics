# Local development profile.
#
# DuckDB is the dev warehouse: no credentials, fast iteration, and dbt models are
# largely portable. 
merchant_settlement:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: ../warehouse.duckdb
      threads: 4

  
